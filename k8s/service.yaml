apiVersion: v1
kind: Service
metadata:
  name: constraint-enforcement-service
  namespace: constraint-enforcement
  labels:
    app: constraint-enforcement-engine
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: constraint-enforcement-engine
